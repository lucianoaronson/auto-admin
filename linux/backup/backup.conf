# Directorios a respaldar (separados por espacio)
SRC_DIRS="/etc /home"

# Destino del backup
DEST_DIR="$PWD/../../docs/evidencias/backups-linux"

# Días de retención (archivos más viejos se borran)
RETAIN_DAYS=3
